language: cpp
compiler:
  - gcc
before_install:
 - sudo apt-get update -qq
 - sudo apt-get install -qq astyle=2.04
script:
 - cd test && make test
 - astyle -X --style=google -n ../**/*.cc ../include/**/*.h | grep Formatted
 - ../script/formatting_checker.sh
